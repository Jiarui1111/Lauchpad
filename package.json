{
  "name": "story-launchpad-subgraph",
  "version": "1.0.0",
  "description": "Subgraph for ip.world launchpad (Story chain) to index new token launches",
  "main": "index.js",
  "scripts": {
    "codegen": "graph codegen --output-dir generated",
    "build": "graph build",
    "deploy:goldsky": "goldsky subgraph deploy story-launchpad-subgraph/0.0.1",
    "deploy:studio": "graph deploy --studio story-launchpad-subgraph",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "subgraph",
    "story",
    "launchpad",
    "ipworld"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.94.0",
    "@graphprotocol/graph-ts": "^0.35.0",
    "assemblyscript": "^0.27.0",
    "matchstick-as": "^0.5.0",
    "typescript": "^5.4.0"
  }
}
